The text fields are not populating once the file is selected! Let's make the following changes: Add a "Load Manifest" button instead of relying on the drop-down.  When this button is clicked, it will process the "manifest.json" file and also look for a "prereqs.json" file to be in the same folder.  Use the manifest.json to fill in the values for "OEM", "Component", "Version", and "Name" text fields in the form.  If the values are not found in the manifest.json, leave blank for the user to fill in.  If the file "prereqs.json" is found in the same folder, set the checkbox for "Enable Prerequisites" to selected.  Leave unchecked if the "prereqs.json" file is not found.  Lastly, remove the "Add Targets" checkbox.  Instead, add a "Targeting" frame with 2 checkboxes: "Stations" and "Programs".  Make sure the saved task.json file also reflects all of these new changes.



Please break up the HTML into easy to navigate and maintain pages. Create a setup tab with the blob connection related items and connect button. Upon successful connection, the other tabs will go from disabled to enabled. Create a "Build" tab with the package selection items. Also include the Manifest details and a small load manifest button that enables only if the manifest file is successfully loaded and parsed. Also create a "Targeting" tab that includes a column with a multi-select list of station (system) names on the left and a column with a multi-select list of program names on the right. Look for a file named "stations.ini" and a file named "programs.ini" to use for populating these values.     
Please break up the single HTML into smaller, HTML files to support tabs. Make sure it is professional, easy to navigate and also to maintain and add pages. Create a "Setup" tab with the blob connection related items and connect button. Upon successful connection, the "Build" tab will change from disabled to enabled. For the "Build" tab, include all other items (package selection, manifest details, load button, all check boxes, and the "Build" button.  When either of the checkboxes for "Stations" or "Programs" are selected, a third tab named "Targeting" should enable and disable when deselected. Make sure to include the Load manifest button that enables only if the manifest file is successfully loaded and parsed. Also create a "Targeting" tab that includes a column with a multi-select list of station (system) names on the left and a column with a multi-select list of program names on the right. Look for a file named "stations.ini" and a file named "programs.ini" to use for populating these values.     
Please break up the single HTML into smaller, HTML files to support tabs. Make sure it is professional, easy to navigate and also to maintain and add pages. There should be an HTML for each tab which should live in a "/pages" subfolder.  Please do not change any functionality, labels, options or controls.  Simply move them into the correct tabs as instructed next. There should be 4 tabs: "Setup", "Build", "Targeting", and "Groups". Create the "Setup" tab with all blob connection related items, Connect button. Upon successful connection, the "Build" tab will change from disabled to enabled. In the "Build" tab, include all other items related to package selection, manifest details, load button, all 3 check boxes, and the "Build" button. Make sure to include a small Load manifest button that enables only if the manifest file is successfully loaded and parsed. Make sure to show the same dialog and export the same JSON from this button.  Also, if any of the checkboxes for "Stations" or "Programs" are selected, the third tab ("Targets") should enable and only disable when both are deselected. Also create a "Targets" tab that includes 2 columns side by side.  Each should have a multi-select list with the left column containing the station (system) names and a column on the right with a multi-select list of program names. Look for a file named "stations.ini" and if present, fill the stations list with these values (keep all ascii).  Also look for a file named "programs.ini" and use for populating these values as well.     
Please break up the single HTML into smaller, HTML files to support tabs. Make sure it is professional, easy to navigate and also to maintain and add pages. There should be an HTML for each tab which should live in a "/pages" subfolder.  Please do not change any functionality, labels, options or controls.  Simply move them into the correct tabs as instructed next. There should be 4 tabs: "Setup", "Build", "Targeting", and "Groups". Create the "Setup" tab with all blob connection related items, Connect button. Upon successful connection, the "Build" tab will change from disabled to enabled. In the "Build" tab, include all other items related to package selection, manifest details, load button, all 3 check boxes, and the "Build" button. Make sure to include a small Load manifest button that enables only if the manifest file is successfully loaded and parsed. Make sure to show the same dialog and export the same JSON in pure ascii from this button.  Also, if any of the checkboxes for "Stations" or "Programs" are selected, the third tab ("Targets") should enable and only disable when both are deselected. For the "Targets" tab, include 2 columns, each with a multi-select list with the left column containing the station (system) names and a column on the right containing program names. Look for a file named "stations.ini" and if present, fill the stations list with these values.  Also look for a file named "programs.ini" and use for populating the second column values as well.
Please break up the single HTML into smaller, HTML files to support tabs. Make sure it is professional, easy to navigate and also to maintain and add pages. There should be an HTML for each tab which should live in a "/pages" subfolder.  Please do not change any functionality, labels, options or controls.  Simply move them into the correct tabs as instructed next. There should be 4 tabs: "Setup", "Build", "Targeting", and "Groups". Create the "Setup" tab with all blob connection related items, Connect button. Upon successful connection, the "Build" tab will change from disabled to enabled. In the "Build" tab, include all other items related to package selection, manifest details, load button, all 3 check boxes, and the "Build" button. Make sure to include a small Load manifest button that enables only if the manifest file is successfully loaded and parsed. Make sure to show the same dialog and export the same JSON in pure ascii from this button.  Also, if any of the checkboxes for "Stations" or "Programs" are selected, the third tab ("Targets") should enable and only disable when both are deselected. For the "Targets" tab, include 2 columns, each with a multi-select list with the left column containing the station (system) names and a column on the right containing program names. Look for a file named "stations.ini" and if present, fill the stations list with these values.  Also look for a file named "programs.ini" and use for populating the second column values as well.

